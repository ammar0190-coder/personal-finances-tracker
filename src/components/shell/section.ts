/**
 * The small-caps section label used across the Dashboard — the quiet
 * counterweight to the serif page title. Shared so sections cannot drift apart.
 */
export const SECTION_LABEL =
  "text-[0.6875rem] font-medium tracking-[0.09em] text-muted-foreground uppercase";

export const SECTION_HEADING = `mb-3 ${SECTION_LABEL}`;
